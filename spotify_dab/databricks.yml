# This is a Databricks asset bundle definition for spotify_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: spotify_dab
  uuid: 33aa4248-00bb-49e9-b7ed-15a3aa61317d

include:
  - resources/*.yml
  - resources/*/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: http://127.0.0.1:7073
    variables:
      catalog: azurespotifyproject
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: http://127.0.0.1:7073
      # We explicitly deploy to /Workspace/Users/dileepc10_outlook.com#ext#@dileepc10outlook.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/dileepc10_outlook.com#ext#@dileepc10outlook.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: azurespotifyproject
      schema: prod
    permissions:
      - user_name: dileepc10_outlook.com#ext#@dileepc10outlook.onmicrosoft.com
        level: CAN_MANAGE
